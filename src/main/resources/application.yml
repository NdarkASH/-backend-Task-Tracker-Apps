spring:
  datasource:
    username: 'darknash'
    password: 'hitambangetjir'
    url: 'jdbc:postgresql://localhost:5432/tracker-list'
    driver-class-name: org.postgresql.Driver




  application:
    name: tracker-list-app
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
       hibernate:
        format_sql: true   # biar lebih rapi
        show_sql: true     # alternatif untuk memastikan selalu tampil
        use_sql_comments: true   # kasih komentar tambahan


